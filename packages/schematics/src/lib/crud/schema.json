{
  "$schema": "http://json-schema.org/schema",
  "id": "CrudSchematic",
  "title": "Crud Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The crud name. To create crud with name Banana in the module fruits you need to introduce fruits/banana",
      "x-prompt": "Introduce the crud name"
    },
    "path": {
      "type": "string",
      "description": "Change the application folder where you will create the crud"
    }
  },
  "required": ["name"]
}
